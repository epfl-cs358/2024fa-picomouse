#include "utils.h"
#include "maze.h"
#include "flood_fill.h"


/**
 * @brief 
 * @param path_1 The first path of the maze traversal (absolute start_cell to absolute exit_cell)
 * @param path_2 The second path of the maze traversal (absolute exit_cell tio absolute start_cell)
 * @return RESULT NULL_PTR / NO_ERROR
 */
RESULT run(PATH_STACK* path_1, PATH_STACK* path_2);
